{
  "name": "siwes",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "git clone https://github.com/bucc-dev/siwes-website-frontend.git && cd siwes-website-frontend && npm install && npm run build && mv ./build ./../ && cd .. && rm -rf siwes-website-frontend && npm install",
    "dev": "export IN_DEV_ENV=true && node --watch --watch-preserve-output app",
    "lint": "npx prettier -w */**"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.20.0",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-fileupload": "^1.4.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.3.5",
    "validator": "^13.7.0"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
